/**
 * @fileOverview Firestore Security Rules for the multi-category info app.
 *
 * Core Philosophy:
 * This ruleset allows public read access to all data items while restricting
 * write access to authenticated users, who are considered administrators.
 *
 * Data Structure:
 * All data is stored in the `/dataItems/{dataItemId}` collection.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Grants public read access to data items, while restricting writes to authenticated users.
     * @path /dataItems/{dataItemId}
     * @allow (get, list): Any user can read data items.
     * @allow (create, update, delete): Only authenticated users can modify data.
     */
    match /dataItems/{dataItemId} {
      allow get: if true;
      allow list: if true;

      allow create: if isSignedIn();
      allow update: if isSignedIn();
      allow delete: if isSignedIn();
    }

    // Helper function to check if a user is signed in.
    function isSignedIn() {
      return request.auth != null;
    }
  }
}
