{
  "entities": {
    "DataItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Data Item",
      "type": "object",
      "description": "Represents a generic data item for various categories like Banks, Universities, or Hospitals, each with its own set of indicators.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the DataItem."
        },
        "name": {
          "type": "string",
          "description": "Name of the item (e.g., 'Banco del Pacífico', 'Universidad San Francisco')."
        },
        "category": {
          "type": "string",
          "description": "The category of the item.",
          "enum": ["Bancos", "Universidades", "Hospitales"]
        },
        "indicators": {
          "type": "object",
          "description": "A map of key-value pairs representing the specific indicators for the item's category.",
          "additionalProperties": {
            "type": "number"
          }
        },
        "color": {
          "type": "string",
          "description": "A hex color code associated with the item for UI visualization."
        }
      },
      "required": [
        "id",
        "name",
        "category",
        "indicators"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/dataItems/{dataItemId}",
        "definition": {
          "entityName": "DataItem",
          "schema": {
            "$ref": "#/backend/entities/DataItem"
          },
          "description": "Stores generic data items for various categories. Publicly readable, admin-writable.",
          "params": [
            {
              "name": "dataItemId",
              "description": "Unique identifier for the data item."
            }
          ]
        }
      }
    ]
  }
}
